#!/bin/bash -e

# コンテナ起動時にマイグレーションを適用する。
# 単一インスタンス運用（Coolify）を前提とした割り切り。複数インスタンスに増やすときは
# デプロイ手順側でマイグレーションを分離すること（docs/DEPLOY.md）。
if [ "${@: -1:1}" == "./bin/rails" ] || [ "${1}" == "./bin/rails" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
