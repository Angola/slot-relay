# CLAUDE.md — 開発ルール（slot-relay）

このリポジトリで作業するときは、必ず以下のルールに従うこと。
プロダクト仕様・アーキテクチャは `docs/DESIGN.md` を参照。

> このファイルは**ルールのハブ**。各ドメインの詳細（バージョン／変更履歴／セキュリティ／設計）は
> 専用ファイルに一次情報を置き、ここからは方針だけを示す。記録は人が読む Markdown
> （plan／milestone／changelog）に一本化する。

## リポジトリ構成

<!-- 実際の構成に合わせて書き換える。例:
- `apps/api/` — バックエンド
- `apps/web/` — フロントエンド
- `packages/` — 共有パッケージ
- `docs/` — 全プロジェクト横断のドキュメント（リポジトリ最上位に集約）
- ルート直下 — `CLAUDE.md` / `VERSION` / `.github`
-->

- **`docs/`** — 全プロジェクト横断のドキュメント・資料は**リポジトリ最上位の `docs/`** に集約する。
- **ルート直下** — `CLAUDE.md` / `VERSION` / `.github`。

## 出力・言語

- **応答・作業ログ・コミットメッセージ・PR は原則すべて日本語で書く**（ユーザーが別言語を指定したときのみ従う）。
- コード内の識別子や外部仕様に沿う固有名詞は英語のままでよい。

## ブランチ運用

1. **作業前は必ず `main` ブランチに切り替え、最新化してから作業ブランチを切る**
   ```bash
   git checkout main && git pull origin main && git checkout -b <branch-name>
   ```
2. `main` への直接 push は禁止。変更は必ず PR 経由で取り込む
3. **マージはスカッシュマージのみ**（Merge commit / Rebase merge は使わない）
4. PR タイトルは Conventional Commits 形式（`feat: ...` / `fix: ...` / `docs: ...` / `chore: ...`）。
   スカッシュマージ後のコミットメッセージがバージョン自動更新の判定に使われる（下記）

> 補足：PR は基本 open で作成する（draft にはしない）。

> **補足（プロジェクトごとに調整）**：上記は「`main` 一本＋作業ブランチ」の最小構成。
> リリースを段階管理したい場合は **`develop` ブランチ運用**（`develop` に集約 → `main` へリリース）にする選択肢もある。
> その場合は分岐元・PR 先を `develop` に読み替え、`version-bump.yml` の対象ブランチ（`on.push.branches`）や
> 自動デプロイ先も合わせて変更すること。**ここは各プロジェクトの規模・リリース頻度に応じて後で見直す前提**の項目。

## バージョニング（自動）

- バージョンは `VERSION` ファイルで管理し、GitHub Actions
  （`.github/workflows/version-bump.yml`）が `main` への push（＝スカッシュマージ）
  ごとに自動で bump・タグ付け・GitHub Release 作成を行う
- bump レベルの判定（コミットメッセージ＝PR タイトル由来）:
  - `BREAKING CHANGE` / `feat!:` 等の `!:` / `[major]` → **major**
  - `feat:` / `[minor]` → **minor**
  - それ以外（`fix:` / `docs:` / `chore:` 等） → **patch**
- 手動でタグを打たない・`VERSION` を手で書き換えない
- バージョン更新をスキップしたい場合はコミットメッセージに `[skip version]` を含める

## ドキュメント

- **ドキュメント・資料はすべて `docs/` 配下に書く**
- チェンジログは `docs/CHANGELOG.md`（Keep a Changelog 形式）。
  作業のたびに `Unreleased` セクションへ追記する
- **`docs/CHANGELOG.md` は `.gitattributes` で `merge=union` を指定している**（コンフリクト対策）:
  - 複数の PR が `Unreleased` に追記しても、Git が両方の行を残して自動マージするため、
    コンフリクトマーカーは原則発生しない（ローカルの merge / rebase / cherry-pick、GitHub 上のマージの両方で有効）
  - `merge=union` は Git 組み込みのマージドライバで、追加設定・インストールは不要
  - 副作用はまれに行順が入れ替わる程度。気づいたらリリース時などに整えればよく、手動解決は原則不要
  - `.gitattributes` のこの指定を削除・変更しないこと
- `docs/` には最低限、以下のファイルを維持する:
  - `DESIGN.md` — プロダクト仕様・アーキテクチャ（一次情報）
  - `MILESTONE.md` — マイルストーン（ざっくりやりたいこと。区切りは自由）
  - `CHANGELOG.md` — チェンジログ
  - `CODE_READING.md` — コードリーディングガイド（構成・読み方）
  - `DEPLOY.md` — デプロイ手順
  - `plans/` — 作業ごとの plan と経緯（背景・方針・意思決定）
- **セキュリティの懸念事項は `docs/SECURITY.md` に書く**。
  新機能・変更でセキュリティ上の懸念が生じたら必ず追記する
- **作業完了時には、該当するドキュメントをすべて最新の状態にアップデートする**

### 記録の 3 分離（やりたいこと / 経緯・決定 / 変更史）

役割の違う「記録」は 1 ファイルに混ぜず、すべて**人が読む Markdown**で次の 3 本に分ける:

- **やりたいこと** — `docs/MILESTONE.md`（ざっくりした目標。区切りは自由・気軽に書き足す）
- **経緯・決定（なぜ）** — `docs/plans/`（作業ごとの plan を 1 ファイルで残す。背景・方針・意思決定を含む）
- **変更史（何を）** — `docs/CHANGELOG.md`（Keep a Changelog・リリース単位・Actions が自動繰り上げ）

> 以前は現在地/決定史を固定スキーマの JSON（`PROJECT_STATUS.json` / `DECISIONS.json`）で管理していたが、
> メンテ負荷で開発が遅くなるため廃止した。記録は上記 Markdown 3 本に一本化する。

## テスト

- **作業完了前に必ずテストを実行し、全テストが通ることを確認する**
- 実装には必ずテストを書く。最低限、以下の観点をカバーすること:
  - **ユニットテスト** — モデル・サービス・関数単位の検証
  - **ユースケースのテスト** — 一連の業務フローの検証
  - **画面遷移のテスト** — E2E で主要導線の遷移を検証（UI がある場合）

## 作業完了チェックリスト

作業を完了とする前に、以下をすべて満たすこと:

- [ ] テストを書いた（ユニット / ユースケース / 画面遷移）
- [ ] 全テストを実行して通った
- [ ] セキュリティ上の懸念を `docs/SECURITY.md` に反映した
- [ ] 該当する docs（`DESIGN.md` / `CODE_READING.md` / `DEPLOY.md` ほか）を更新した
- [ ] `docs/CHANGELOG.md` の `Unreleased` に変更内容を追記した
- [ ] 作業の背景・方針・意思決定を `docs/plans/` に plan として残した
- [ ] 必要なら `docs/MILESTONE.md` を更新した
- [ ] PR を作成した（スカッシュマージ前提・Conventional Commits 形式のタイトル）

## 進め方（plan 起点）

- **何かをやるときは plan から始める。** まず `docs/plans/` に plan を 1 ファイル作り、
  「やりたいこと・背景（なぜ）・方針・経緯」を書く（詳細は `docs/plans/README.md`）。
- 作業が終わっても plan は消さず、経緯・意思決定の一次記録として残す。
- ざっくりした目標は `docs/MILESTONE.md` に、変更履歴は `docs/CHANGELOG.md` に、
  設計の一次情報は `docs/DESIGN.md` に置く（役割を混ぜない）。
- 現状に合わない記述は放置せず必ず直す。不明な情報は推測せず `unknown` と記録する。
